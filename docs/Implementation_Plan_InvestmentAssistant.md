# Детализированный план реализации Investment Assistant

## 1. Цель и границы реализации

- Цель: реализовать прототип инвестиционного ассистента по архитектуре `LangGraph + MCP` с тремя MCP-серверами, оркестратором и двумя UI.
- Границы: только требования из `docs/TZ_InvestmentAssistant.md` версии 1.0.
- Ключевые результаты:
  - Рабочие MCP-сервисы `market_server`, `news_server`, `analytics_server`.
  - Рабочий оркестратор с маршрутизацией запросов и суммаризацией ответа.
  - UI: CLI и Streamlit.
  - Покрытие тестами: юнит + интеграционные + E2E-сценарии.
  - Соблюдение требований безопасности (SQL, prompt injection, секреты).

## 2. Принципы декомпозиции работ

- Каждая задача декомпозирована до минимально исполнимого шага.
- Каждая подзадача имеет проверяемый артефакт.
- Внутри модулей: сначала интерфейсы/контракты, затем реализация, затем тесты.
- Нефункциональные требования (логирование, ошибки, таймауты, кэш) внедряются сразу, а не в конце.

## 3. Критерии готовности проекта (Definition of Done)

- Все модули соответствуют структуре проекта из ТЗ.
- Все публичные функции типизированы и снабжены docstring (Google-стиль).
- MCP-инструменты имеют корректные docstring-описания для LLM.
- Все настройки берутся только из `config/settings.py`.
- Нет использования `print` и `logging.basicConfig`, только `structlog`.
- Проходят:
  - юнит-тесты;
  - интеграционные тесты;
  - E2E-сценарии из ТЗ.
- Реализованы ограничения безопасности:
  - `execute_analytics_query` исполняет только `SELECT`;
  - ограничение `LIMIT 1000`;
  - таймауты и read-only режимы;
  - экранирование потенциально инъекционного новостного контента.

## 4. План работ (WBS)

## Этап 0. Инициация и подготовка среды

### 0.1 Базовый аудит репозитория

- [ ] Проверить наличие и состояние целевой структуры каталогов.
- [ ] Выявить уже реализованные модули и зафиксировать gap к ТЗ.
- [ ] Проверить наличие `pyproject.toml`, `.env.example`, базового `README.md`.
- [ ] Проверить согласованность имен модулей с ТЗ.

### 0.2 Настройка зависимостей и инструментов

- [ ] Добавить/актуализировать зависимости в `pyproject.toml` через `uv`:
  - `langgraph`, `fastmcp`, `apimoex`, `requests`, `clickhouse-connect`,
  - `sqlglot`, `feedparser`, `pydantic-settings`, `structlog`,
  - `numpy`, `scipy`, `streamlit`, `typer`, `pytest`, `pytest-asyncio`.
- [ ] Проверить группы dev-зависимостей (если используются).
- [ ] Выполнить синхронизацию lock-файла.
- [ ] Проверить, что проект запускается в чистой среде.

### 0.3 Конфигурационный каркас

- [ ] Создать/обновить `config/settings.py` с `BaseSettings`.
- [ ] Вынести в settings все параметры из ТЗ (LLM, ClickHouse, TTL, recursion, mock).
- [ ] Добавить функцию `get_settings()` с кэшированием.
- [ ] Убедиться, что ни один модуль не читает `os.environ` напрямую.
- [ ] Сформировать `.env.example` с обязательными переменными.

---

## Этап 1. Данные и инфраструктура хранилища

### 1.1 DDL-миграции ClickHouse

- [ ] Создать `data/migrations/001_initial.sql`.
- [ ] Описать таблицу `portfolios` в соответствии с ТЗ.
- [ ] Описать таблицу `price_history` в соответствии с ТЗ.
- [ ] Описать таблицу `bond_details` в соответствии с ТЗ.
- [ ] Проверить корректность типов и ключей сортировки.

### 1.2 Фикстуры

- [ ] Подготовить `data/fixtures/portfolio_sample.json` (10 позиций по ТЗ).
- [ ] Подготовить `data/fixtures/moex_candles_sample.json` (2 года свечей).
- [ ] Проверить, что тикеры в фикстурах согласованы между файлами.
- [ ] Добавить валидацию структуры фикстур (минимум на уровне теста).

### 1.3 Клиент доступа к данным аналитики

- [ ] Реализовать `servers/analytics_server/clickhouse_client.py`.
- [ ] Добавить инициализацию read-only клиента `clickhouse_connect`.
- [ ] Добавить таймауты на запросы.
- [ ] Реализовать mock-режим чтения из фикстур (`use_mock_clickhouse`).
- [ ] Реализовать единый интерфейс доступа к данным для real/mock режимов.

---

## Этап 2. Реализация analytics_server

### 2.1 Расчет портфельной сводки

- [ ] Реализовать загрузку позиций по `portfolio_id`.
- [ ] Реализовать получение последних цен по тикерам.
- [ ] Реализовать расчет:
  - [ ] `market_value` по позиции;
  - [ ] `pnl` и `pnl_pct`;
  - [ ] веса позиций.
- [ ] Реализовать агрегаты аллокации:
  - [ ] `by_sector`;
  - [ ] `by_type`;
  - [ ] `by_currency`.
- [ ] Сформировать финальную структуру ответа `get_portfolio_summary`.

### 2.2 Расчеты риск-метрик (`risk_calculator.py`)

- [ ] Реализовать извлечение исторических цен за 252 дня.
- [ ] Реализовать расчет дневных доходностей по инструментам.
- [ ] Реализовать расчет портфельной доходности (взвешенная серия).
- [ ] Реализовать исторический VaR.
- [ ] Реализовать параметрический VaR.
- [ ] Реализовать CVaR.
- [ ] Реализовать аннуализированную волатильность.
- [ ] Реализовать Sharpe (с risk-free на основе ключевой ставки / 252).
- [ ] Реализовать Max Drawdown.
- [ ] Реализовать HHI:
  - [ ] по позициям;
  - [ ] по секторам.
- [ ] Добавить интерпретационные текстовые поля к метрикам.

### 2.3 Стресс-тестирование (`stress_tester.py`)

- [ ] Реализовать сценарий `index_drop`:
  - [ ] оценка beta каждой позиции к IMOEX (OLS, 252 дня);
  - [ ] перерасчет потерь по каждой позиции;
  - [ ] агрегирование общих потерь.
- [ ] Реализовать сценарий `rate_hike`:
  - [ ] переоценка облигаций по дюрации;
  - [ ] дополнительная просадка акций чувствительных секторов;
  - [ ] расчет итогового эффекта.
- [ ] Реализовать сценарий `sector_decline`:
  - [ ] фильтрация по целевому сектору;
  - [ ] пересчет потерь только по затронутым позициям.
- [ ] Сформировать унифицированный ответ `run_stress_test`.

### 2.4 Безопасный SQL-инструмент

- [ ] Реализовать `execute_analytics_query`.
- [ ] Добавить AST-парсинг через `sqlglot.parse_one`.
- [ ] Запретить все не-SELECT запросы.
- [ ] Добавить проверку на запрещенные ключевые слова.
- [ ] Добавить авто-добавление `LIMIT 1000`.
- [ ] Добавить ограничение таймаута 30 секунд.
- [ ] Гарантировать использование read-only подключения.
- [ ] Подготовить структуру ответа: `columns`, `rows`, `row_count`.

### 2.5 MCP-сервер (`servers/analytics_server/server.py`)

- [ ] Поднять FastMCP сервер.
- [ ] Зарегистрировать инструменты:
  - [ ] `get_portfolio_summary`;
  - [ ] `calculate_risk_metrics`;
  - [ ] `run_stress_test`;
  - [ ] `execute_analytics_query`.
- [ ] Добавить docstring для каждого инструмента.
- [ ] Добавить логирование `structlog` на входе и при ошибках.
- [ ] Обернуть ожидаемые ошибки в `ToolError`.

### 2.6 Тесты analytics_server

- [ ] Юнит: корректность `VaR/CVaR/volatility/Sharpe`.
- [ ] Юнит: корректность `Max Drawdown` и `HHI`.
- [ ] Юнит: стресс-сценарии на синтетических данных.
- [ ] Юнит: SQL-валидация (разрешенные и запрещенные запросы).
- [ ] Интеграционный smoke: вызов всех MCP-инструментов в mock-режиме.

---

## Этап 3. Реализация market_server

### 3.1 Клиент MOEX (`moex_client.py`)

- [ ] Реализовать обертку над `apimoex` + `requests.Session`.
- [ ] Реализовать единый обработчик ошибок внешних вызовов.
- [ ] Настроить таймауты HTTP-запросов.
- [ ] Реализовать in-memory TTL-кэш:
  - [ ] 60 секунд для котировок;
  - [ ] 300 секунд для исторических данных.
- [ ] Реализовать нормализацию данных MOEX в целевые схемы ответа.

### 3.2 Инструменты market_server

- [ ] `get_stock_quote`:
  - [ ] валидация тикера;
  - [ ] поиск на `TQBR`;
  - [ ] формирование нужных полей;
  - [ ] `ToolError` для отсутствующего тикера.
- [ ] `get_candles`:
  - [ ] валидация формата дат;
  - [ ] валидация диапазона (<= 5 лет);
  - [ ] валидация интервала;
  - [ ] формирование массива свечей.
- [ ] `get_board_securities`:
  - [ ] валидация board (`TQBR`, `TQCB`, `TQOB`);
  - [ ] возврат `SECID`, `SHORTNAME`, `LOTSIZE`, `PREVPRICE`.
- [ ] `get_index_analytics`:
  - [ ] валидация индекса (`IMOEX`, `RTSI`, `RGBI`);
  - [ ] получение значения и изменения;
  - [ ] получение компонентов и весов.
- [ ] `get_bond_data`:
  - [ ] поиск по `TQCB` и `TQOB`;
  - [ ] возврат параметров облигации из ТЗ.

### 3.3 MCP-сервер и аннотации

- [ ] Реализовать `servers/market_server/server.py`.
- [ ] Проставить аннотации MCP (`readOnlyHint`, `idempotentHint`) где требуется.
- [ ] Добавить единообразное логирование входных параметров.
- [ ] Обработать все исключения через `ToolError`.

### 3.4 Тесты market_server

- [ ] Мокнуть `apimoex` ответы для каждого инструмента.
- [ ] Проверить поведение кэша (hit/miss/expiration).
- [ ] Проверить `ToolError` на неверном тикере.
- [ ] Проверить валидации дат, интервалов, board/index.

---

## Этап 4. Реализация news_server

### 4.1 RSS-агрегатор (`rss_fetcher.py`)

- [ ] Задать конфигурацию источников и trust-priority (HIGH/MEDIUM/LOW).
- [ ] Реализовать загрузку RSS с таймаутом 10 секунд.
- [ ] Реализовать in-memory кэш RSS (TTL 300 секунд).
- [ ] Реализовать отказоустойчивость:
  - [ ] лог warning при недоступности источника;
  - [ ] продолжение обработки доступных источников.
- [ ] Реализовать унификацию структуры новостей по источникам.

### 4.2 Эвристическая тональность (`sentiment.py`)

- [ ] Определить словари позитивных/негативных маркеров.
- [ ] Реализовать функцию классификации `positive|negative|neutral`.
- [ ] Реализовать вычисление агрегированного sentiment score.
- [ ] Покрыть граничные случаи (пустой текст, смешанные маркеры).

### 4.3 Инструменты news_server

- [ ] `fetch_news`:
  - [ ] фильтрация по `query`;
  - [ ] фильтрация по выбранным источникам;
  - [ ] дедупликация по `title + source`;
  - [ ] сортировка по дате (desc);
  - [ ] ограничение `limit`;
  - [ ] включение полей `source_trust` и `sentiment`.
- [ ] `get_cb_key_rate`:
  - [ ] извлечение текущей ставки;
  - [ ] извлечение даты действия и следующего заседания;
  - [ ] fallback-логика при частичной недоступности источников.
- [ ] `get_market_sentiment`:
  - [ ] сбор релевантных новостей за 7 дней;
  - [ ] подсчет positive/negative/neutral;
  - [ ] расчет `score` в диапазоне [-1, +1];
  - [ ] выделение `top_headlines`.
- [ ] `get_macro_calendar`:
  - [ ] сбор событий в диапазоне дат;
  - [ ] структура `date/event/impact/previous`.

### 4.4 MCP-сервер (`servers/news_server/server.py`)

- [ ] Зарегистрировать все инструменты в FastMCP.
- [ ] Добавить docstring-описания для MCP.
- [ ] Гарантировать отсутствие вызовов LLM внутри `news_server`.
- [ ] Логировать вызовы и ошибки через `structlog`.

### 4.5 Тесты news_server

- [ ] Юнит: разбор RSS (моки `feedparser`).
- [ ] Юнит: корректность sentiment эвристики.
- [ ] Юнит: дедупликация и сортировка новостей.
- [ ] Юнит: устойчивость к недоступному источнику.
- [ ] Интеграционный smoke: вызов всех инструментов.

---

## Этап 5. Оркестратор LangGraph

### 5.1 Состояние и схемы (`orchestrator/state.py`)

- [ ] Реализовать `InvestmentAssistantState` (TypedDict) строго по ТЗ.
- [ ] Проверить корректность типов полей.
- [ ] Добавить начальное состояние (фабрика `initial_state` при необходимости).

### 5.2 Узел ввода (`input_node.py`)

- [ ] Реализовать валидацию непустого запроса.
- [ ] Реализовать ограничение длины <= 2000.
- [ ] Реализовать извлечение тикеров regex-шаблоном.
- [ ] Реализовать классификацию `query_type` по ключевым словам.
- [ ] Реализовать классификацию `complex` при множественных совпадениях.

### 5.3 Планировщик (`planner_node.py`)

- [ ] Описать `PlanStep` и `PlanSchema` (Pydantic).
- [ ] Подготовить системный промпт для планировщика.
- [ ] Реализовать structured output вызов LLM.
- [ ] Реализовать ветку первого входа (`plan == []`): генерация плана.
- [ ] Реализовать ветку повторного входа: выбор следующего шага.
- [ ] Добавить контроль лимита итераций (max 10).
- [ ] Добавить защиту от некорректного шага плана.

### 5.4 Executor-узлы

- [ ] `market_executor`:
  - [ ] выполнение tool-call через MCP клиент;
  - [ ] retry 1/2/4 сек для временных ошибок 5xx;
  - [ ] исключение из retry для ошибок "тикер не найден";
  - [ ] инкремент `error_count` при ошибках;
  - [ ] обновление `market_data`.
- [ ] `news_executor`:
  - [ ] вызов news-инструментов;
  - [ ] деградация при недоступности новостей (после порога ошибок);
  - [ ] добавление предупреждения в сообщения;
  - [ ] обновление `news_data`.
- [ ] `analytics_executor`:
  - [ ] вызов analytics-инструментов;
  - [ ] fallback на упрощенный расчет при сбое risk-metrics;
  - [ ] обновление `portfolio_metrics`.

### 5.5 Узел суммаризации (`summarizer_node.py`)

- [ ] Подготовить системный промпт суммаризатора.
- [ ] Передавать данные с указанием источников и таймстемпов.
- [ ] Передавать trust-weight новостных источников.
- [ ] Добавить инструкцию формата ответа: заголовок/факты/риски/итог.
- [ ] Заполнять `final_answer`.

### 5.6 Граф и маршрутизация (`graph.py`)

- [ ] Добавить все узлы в `StateGraph`.
- [ ] Настроить ребра `START -> input -> planner`.
- [ ] Реализовать `route_planner`.
- [ ] Настроить возврат executor-узлов в planner.
- [ ] Настроить переход `summarizer -> END`.
- [ ] Собрать граф с checkpointer (`MemorySaver`).
- [ ] Установить `recursion_limit` из конфигурации.

### 5.7 MCP клиент (`mcp_client.py`)

- [ ] Реализовать обертку над `MultiServerMCPClient`.
- [ ] Описать конфиг всех серверов через settings.
- [ ] Реализовать запуск серверов как stdio-подпроцессов.
- [ ] Реализовать загрузку tools один раз на старте приложения.
- [ ] Обработать ошибки и недоступность серверов.

### 5.8 Промпты (`prompts.py`)

- [ ] Создать системный промпт planner.
- [ ] Создать системный промпт summarizer.
- [ ] Добавить правила защиты от prompt injection.
- [ ] Добавить шаблоны форматирования для стабилизации output.

### 5.9 Тесты оркестратора

- [ ] Юнит: классификация `input_node`.
- [ ] Юнит: маршрутизация `planner` по target_server.
- [ ] Юнит: retry-логика `market_executor`.
- [ ] Юнит: деградация `news_executor` при отказах.
- [ ] Юнит: fallback `analytics_executor`.
- [ ] Интеграционный тест графа на mock MCP.

---

## Этап 6. Пользовательские интерфейсы

### 6.1 CLI (`ui/cli.py`)

- [ ] Реализовать команду `chat`.
- [ ] Реализовать REPL-цикл.
- [ ] Реализовать команды `exit`, `clear`, `debug`.
- [ ] Выводить ответы оркестратора с сохранением читаемого формата.
- [ ] Добавить обработку ошибок на уровне UX (без stacktrace пользователю).

### 6.2 Streamlit (`ui/streamlit_app.py`)

- [ ] Добавить текстовое поле запроса.
- [ ] Отрисовывать ответ в Markdown.
- [ ] Реализовать sidebar:
  - [ ] выбор `portfolio_id`;
  - [ ] выбор модели LLM.
- [ ] Реализовать хранение истории диалога через `session_state`.
- [ ] Добавить spinner на время выполнения запроса.
- [ ] Добавить отображение мягких предупреждений (например, news degraded).

### 6.3 UI smoke-тесты

- [ ] Проверить запуск CLI и Streamlit без падений.
- [ ] Проверить один сценарий на каждый тип запроса.
- [ ] Проверить корректность отображения ошибок и пустых ответов.

---

## Этап 7. Безопасность и надежность

### 7.1 SQL-безопасность

- [ ] Проверить, что `execute_analytics_query` пропускает только SELECT.
- [ ] Проверить блокировку DDL/DML ключевых слов.
- [ ] Проверить автодобавление `LIMIT 1000`.
- [ ] Проверить таймаут и корректную ошибку при долгих запросах.

### 7.2 Prompt injection защита

- [ ] Реализовать экранирование новостного контента (`<`, `>`, `{`, `}`).
- [ ] Проверить, что user-input не переопределяет system prompt planner.
- [ ] Проверить ограничение длины запроса в `input_node`.

### 7.3 Секреты и конфигурация

- [ ] Убедиться, что `.env` добавлен в `.gitignore`.
- [ ] Проверить отсутствие логирования секретов.
- [ ] Проверить, что все секреты читаются только из settings.

### 7.4 Отказоустойчивость

- [ ] Проверить деградацию при падении одного из RSS-источников.
- [ ] Проверить деградацию при временных ошибках MOEX API.
- [ ] Проверить поведение при недоступном ClickHouse (mock fallback).

---

## Этап 8. Тестирование и верификация

### 8.1 Юнит-тесты по модулям

- [ ] Закрыть тестами все MCP-инструменты.
- [ ] Закрыть тестами критические расчетные функции.
- [ ] Закрыть тестами узлы и роутинг оркестратора.
- [ ] Добавить негативные тесты на валидацию входов и ошибки.

### 8.2 Интеграционные тесты

- [ ] Поднять связку оркестратор + mock/real MCP.
- [ ] Проверить корректный выбор инструментов.
- [ ] Проверить целостность данных между узлами state.

### 8.3 E2E-сценарии из ТЗ (`tests/test_e2e.py`)

- [ ] Сценарий 1: "Покажи котировку SBER".
- [ ] Сценарий 2: "Последние новости по Газпрому".
- [ ] Сценарий 3: "Оцени риск портфеля demo_portfolio".
- [ ] Сценарий 4: "Стресс-тест: IMOEX -20%".
- [ ] Сценарий 5: "Оцени портфель с учетом новостей нефтегаза".

### 8.4 Метрики качества (для диплома)

- [ ] Добавить логирование:
  - [ ] `scenario_success`;
  - [ ] `tool_selection_correct`;
  - [ ] `response_time_sec`;
  - [ ] `mcp_calls_count`;
  - [ ] `error_count_final`.
- [ ] Реализовать сбор метрик в удобном формате (JSONL/таблица).
- [ ] Подготовить скрипт формирования сводного отчета по сценариям.

---

## Этап 9. Документация и финализация

### 9.1 Документация разработчика

- [ ] Обновить `README.md`:
  - [ ] запуск среды;
  - [ ] запуск каждого MCP-сервера;
  - [ ] запуск CLI;
  - [ ] запуск Streamlit;
  - [ ] запуск тестов.
- [ ] Добавить описание переменных окружения.
- [ ] Добавить раздел известных ограничений прототипа.

### 9.2 Техническая документация по архитектуре

- [ ] Описать потоки данных между слоями.
- [ ] Описать контракты MCP-инструментов.
- [ ] Описать state-переходы LangGraph.
- [ ] Добавить диаграмму последовательности для комплексного запроса.

### 9.3 Предзащита и приемка

- [ ] Подготовить демонстрационные сценарии.
- [ ] Проверить воспроизводимость запуска на чистой машине.
- [ ] Подготовить чеклист показа для научного руководителя.

---

## 5. Матрица зависимостей между этапами

- Этап 0 обязателен перед всеми остальными.
- Этап 1 обязателен перед полной реализацией `analytics_server`.
- Этапы 2, 3, 4 могут идти частично параллельно после Этапа 0.
- Этап 5 стартует после появления минимально рабочих MCP-серверов.
- Этап 6 стартует после базовой работоспособности оркестратора.
- Этап 7 и 8 идут итеративно, но финальная приемка только после полного прохождения.
- Этап 9 выполняется после стабилизации функционала.

## 6. Рекомендуемые итерации (спринты)

### Спринт 1 (инфраструктура и аналитика)

- Этапы: 0 + 1 + 2.
- Цель: получить автономно работающий `analytics_server` с тестами.

### Спринт 2 (рыночные и новостные данные)

- Этапы: 3 + 4.
- Цель: закрыть `market_server` и `news_server` с кэшем и отказоустойчивостью.

### Спринт 3 (оркестрация)

- Этап: 5.
- Цель: собрать LangGraph, подключить MCP, получить связный ответ.

### Спринт 4 (UI и hardening)

- Этапы: 6 + 7.
- Цель: пользовательские интерфейсы и закрытие требований безопасности.

### Спринт 5 (валидация и дипломная упаковка)

- Этапы: 8 + 9.
- Цель: полнота тестов, метрики качества, документация и демонстрация.

## 7. Риск-реестр реализации

- [ ] Риск: нестабильность внешних API (MOEX, RSS).
  - Митигация: кэш, retry, graceful degradation, фикстуры.
- [ ] Риск: низкая воспроизводимость расчетов из-за разнородных данных.
  - Митигация: детерминированные тестовые данные, фиксированные сценарии.
- [ ] Риск: некорректная маршрутизация planner.
  - Митигация: unit-тесты routing + golden-case тесты.
- [ ] Риск: SQL-инъекции в `execute_analytics_query`.
  - Митигация: AST-валидация, запрет DML/DDL, read-only user, timeout.
- [ ] Риск: задержки ответа при комплексных запросах.
  - Митигация: ограничение шагов, параллелизм внутри узлов, кэш инструментов.

## 8. Мини-чеклист перед релизом v1 (Go/No-Go)

- [ ] Все MCP-инструменты отвечают по контрактам ТЗ.
- [ ] Комплексный запрос задействует правильные серверы.
- [ ] Ошибки обрабатываются без падения графа.
- [ ] SQL-защита подтверждена негативными тестами.
- [ ] UI стабильно работает на 5 E2E-сценариях.
- [ ] Зафиксированы метрики качества для главы диплома.
- [ ] README и инструкции запуска актуальны.

